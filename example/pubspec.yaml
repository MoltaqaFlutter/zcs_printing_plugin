name: zcs_printing_example
description: Example app for zcs_printing plugin
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  zcs_printing:
    path: ../
  image_picker: ^1.0.7
  file_picker: ^8.1.2
  typed_data: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
